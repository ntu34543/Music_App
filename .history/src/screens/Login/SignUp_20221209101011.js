import React from 'react';
import {
    Button,
    SafeAreaView,
    ScrollView,
    StyleSheet,
    Text,
    View,
  } from 'react-native';

function SignUp() {
  return (
    // Trang resgister or sign up trong Figma
    <SafeAreaView>Làm vào trong đây nha</SafeAreaView>
  )
}

const styles = StyleSheet.create({
    item: {
      paddingVertical: 20,
      marginBottom: 10,
      marginHorizontal: 20,
      backgroundColor: 'pink',
    },
  });

export default SignUp