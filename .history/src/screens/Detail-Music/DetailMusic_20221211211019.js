import React, {useState} from 'react';
import {
  StyleSheet,
  Text,
  View,
  Image,
  ScrollView,
  FlatList,
} from 'react-native';

export default function DetailMusic({id}) {
  return (
    <View style={styles.container}>
      
    </View>
  );
}
// Satoshi
const styles = StyleSheet.create({
  
});
